{
  "name": "bungie_api_proxy",
  "version": "0.0.1",
  "description": "Proxies requests to the bungie API through the user's browser in order to make authenticated requests without transmitting the cookie to untrusted endpoints",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jquery": "^2.2.4",
    "ramda": "^0.21.0"
  },
  "devDependencies": {
    "watchify": "^3.7.0",
    "browserify": "^13.0.1"
  }
}
